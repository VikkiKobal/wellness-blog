GOOGLE_APPLICATION_CREDENTIALS=./wellness-website-b150a-firebase-adminsdk-fbsvc-9fb11202a0.json
FIREBASE_PROJECT_ID=wellness-website-b150a
CORS_ORIGINS=http://localhost:4321

# ---- Contact form email (SMTP) ----
# The frontend POSTs to: ${PUBLIC_API_URL}/contact (defaults to http://localhost:8080/api/contact)
# Configure SMTP + recipient to enable email sending.

# Who receives contact form emails
CONTACT_TO=you@yourdomain.com

# The "From" header used when sending (must be allowed by your SMTP provider)
CONTACT_FROM=no-reply@yourdomain.com

# Optional prefix for email subject lines
CONTACT_SUBJECT_PREFIX=[Galactic Giant]

# SMTP server config (examples below)
# - Gmail/Google Workspace often uses: host=smtp.gmail.com port=587 (STARTTLS)
# - Some providers use port=465 (implicit TLS)
SMTP_HOST=smtp.yourprovider.com
SMTP_PORT=587
SMTP_USER=your-smtp-username
SMTP_PASS=your-smtp-password

# Optional:
# SMTP_IMPLICIT_TLS=true          # usually only needed for port 465
# SMTP_TLS_INSECURE_SKIP_VERIFY=false