---

---

<section id="hero" class="hero">
  <div class="hero-bg">
    <div class="hero-circle-outline"></div>
    <div class="hero-circle-gradient"></div>
    <div class="hero-image-wrapper">
      <img src="/images/hero-portrait.png" alt="Antonina Devitska" class="hero-image" />
    </div>
  </div>
  
  <div class="container hero-container">
    <div class="hero-content">
      <div class="hero-heading">
        <h1>
          <span class="line-1">Human-Centered <span class="teaching-word">Teaching<svg class="heading-underline" width="140" height="10" viewBox="0 0 140 10" fill="none" xmlns="http://www.w3.org/2000/svg">
              <path d="M2 8C30 2 110 -2 138 8" stroke="#BAD8B6" stroke-width="4" stroke-linecap="round"/>
            </svg></span></span>
          <span class="line-2">for a <span class="teaching-word">Changing<svg class="heading-underline" width="140" height="10" viewBox="0 0 140 10" fill="none" xmlns="http://www.w3.org/2000/svg">
              <path d="M2 8C30 2 110 -2 138 8" stroke="#BAD8B6" stroke-width="4" stroke-linecap="round"/>
            </svg></span> World</span>
        </h1>
      </div>
      
      <p class="hero-description">
        I am an educator, mentor, and course designer helping teachers, universities, and institutions bring well-being, resilience, and innovation into the learning process.
        My work bridges research, global experience, and mindful, trauma-informed practice
              </p>
      
      <div class="hero-actions">
        <a href="/courses" class="btn btn-primary hero-btn">
          Explore my work
        </a>
        <a href="/courses" class="btn btn-primary hero-btn">
          Invite me to speak
        </a>
      </div>
      
    </div>
  </div>
</section>

<style>
  .hero {
    position: relative;
    min-height: calc(100vh - 80px);
    padding: 60px 0 80px;
    overflow: hidden;
  }
  
  .hero-bg {
    position: absolute;
    top: 0;
    right: 0;
    width: 60%;
    height: 100%;
    pointer-events: none;
  }
  
  .hero-circle-outline {
    position: absolute;
    top: 110px;
    right: -20px;
    width: 560px;
    height: 560px;
    border: 3px solid var(--color-primary);
    border-radius: 50%;
    opacity: 0.5;
  }
  
  .hero-circle-gradient {
    position: absolute;
    top: 135px;
    right: 5px;
    width: 510px;
    height: 510px;
    border-radius: 50%;
    background: var(--gradient-hero);
    opacity: 0.8;
  }
  
  .hero-image-wrapper {
    position: absolute;
    top: 135px;
    right: 5px;
    width: 510px;
    height: 510px;
    z-index: 2;
    border-radius: 50%;
    overflow: hidden;
  }

  @media (min-width: 1270px) {
    .hero-circle-outline {
      right: -20px;
    }
    .hero-circle-gradient,
    .hero-image-wrapper {
      right: 5px;
    }
  }

  @media (min-width: 1285px) {
    .hero-circle-outline {
      right: 30px;
    }
    .hero-circle-gradient,
    .hero-image-wrapper {
      right: 55px;
    }
  }

  @media (min-width: 1290px) {
    .hero-circle-outline {
      right: 60px;
    }
    .hero-circle-gradient,
    .hero-image-wrapper {
      right: 85px;
    }
  }

  .hero-image {
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center top;
  }

  .hero-container {
    position: relative;
    z-index: 3;
  }

  .hero-content {
    max-width: 658px;
    padding-top: 60px;
  }

  .hero-heading {
    margin-bottom: 40px;
  }

  .hero-heading h1 {
    font-family: var(--font-primary);
    font-weight: 600;
    font-size: 50px;
    line-height: 1.25;
    color: var(--color-text-dark);
  }

  .hero-heading .line-1 {
    display: block;
  }

  .hero-heading .line-2 {
    display: block;
  }

  .teaching-word {
    position: relative;
    display: inline-block;
    z-index: 1;
  }

  .heading-underline {
    position: absolute;
    bottom: 2px;
    left: 0;
    width: 140px;
    height: 10px;
    z-index: -1;
  }
  
  .hero-description {
    font-family: var(--font-primary);
    font-weight: 400;
    font-size: 22px;
    line-height: 1.39;
    color: var(--color-text-black);
    margin-bottom: 50px;
    max-width: 650px;
  }

  .hero-actions {
    display: flex;
    flex-wrap: wrap;
    gap: 14px;
    align-items: center;
  }
  
  .hero-btn {
    background: var(--color-primary);
    color: var(--color-white);
    padding: 12px 22px;
    font-size: 20px;
  }
  
  /* Tablet landscape / Small Desktop: 969px - 1270px */
  @media (max-width: 1270px) and (min-width: 969px) {
    .hero {
      padding: 130px 0 70px;
    }
    
    .hero-container {
      padding-left: clamp(40px, 5vw, 80px);
    }
    
    .hero-bg {
      width: 50%;
    }
    
    .hero-circle-outline {
      width: clamp(380px, 40vw, 480px);
      height: clamp(380px, 40vw, 480px);
      top: 160px;
      right: 40px;
    }
    
    .hero-circle-gradient,
    .hero-image-wrapper {
      width: calc(clamp(380px, 40vw, 480px) - 40px);
      height: calc(clamp(380px, 40vw, 480px) - 40px);
      top: 180px;
      right: 60px;
    }
    
    .hero-content {
      max-width: clamp(400px, 45vw, 540px);
      padding-top: 40px;
    }
    
    .hero-heading h1 {
      font-size: clamp(32px, 4vw, 44px);
    }
    
    .hero-description {
      font-size: clamp(16px, 1.8vw, 20px);
      margin-bottom: 35px;
    }
    
    .hero-btn {
      font-size: 17px;
      padding: 11px 20px;
    }
  }
  
  @media (max-width: 968px) {
    .hero {
      min-height: auto;
      padding: 120px 0 60px;
    }
    
    .hero-bg {
      position: relative;
      width: 100%;
      height: 350px;
      order: 1;
      display: flex;
      justify-content: center;
      align-items: center;
      margin-bottom: 40px;
    }
    
    .hero-circle-outline {
      width: 300px;
      height: 300px;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
      right: auto;
    }
    
    .hero-circle-gradient,
    .hero-image-wrapper {
      width: 260px;
      height: 260px;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
      right: auto;
    }
    
    .hero-container {
      display: flex;
      flex-direction: column;
    }
    
    .hero-content {
      max-width: 100%;
      padding-top: 0;
      padding-left: 24px;
      padding-right: 24px;
      text-align: center;
    }
    
    .hero-heading h1 {
      font-size: 34px;
    }
    
    .heading-underline {
      display: none;
    }
    
    .hero-description {
      font-size: 17px;
      margin: 0 auto 40px;
    }

    .hero-btn {
      font-size: 18px;
      padding: 12px 20px;
    }

    .hero-actions {
      justify-content: center;
    }
  }
  
  @media (max-width: 480px) {
    .hero {
      padding: 104px 0 48px;
    }

    .hero-bg {
      height: 280px;
      margin-bottom: 32px;
    }
    
    .hero-circle-outline {
      width: 260px;
      height: 260px;
    }
    
    .hero-circle-gradient,
    .hero-image-wrapper {
      width: 220px;
      height: 220px;
    }
    
    .hero-content {
      padding-left: 20px;
      padding-right: 20px;
    }

    .hero-heading h1 {
      font-size: 26px;
    }
    
    .hero-description {
      font-size: 15px;
      margin-bottom: 36px;
    }
    
    .hero-btn {
      width: 100%;
      font-size: 16px;
      padding: 12px 16px;
    }
  }
</style>

