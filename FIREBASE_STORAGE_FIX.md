# 🔧 Виправлення завантаження зображень

## Проблема
Зображення не завантажуються - застрягають на "Uploading..."

## Рішення

### 1. Перевірте консоль браузера (F12)

Натисніть **F12** в браузері і подивіться на вкладку **Console**. Ви побачите одну з помилок:

#### Помилка A: `storage/unauthorized`
```
Error: Firebase Storage: User does not have permission to access 'articles/...'
```

**Рішення:** Оновіть Storage Rules

#### Помилка B: `Firebase Storage is not initialized`
```
Error: Firebase Storage is not initialized
```

**Рішення:** Увімкніть Firebase Storage в консолі

---

### 2. Налаштування Firebase Storage Rules

1. Відкрийте [Firebase Console](https://console.firebase.google.com/)
2. Виберіть ваш проект
3. В меню зліва: **Build** → **Storage**
4. Перейдіть на вкладку **Rules**
5. Замініть правила на:

```javascript
rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Allow public read
    match /{allPaths=**} {
      allow read: if true;
    }
    
    // Allow write only for authenticated users
    match /articles/{fileName} {
      allow write: if request.auth != null;
    }
    
    match /courses/{fileName} {
      allow write: if request.auth != null;
    }
  }
}
```

6. Натисніть **"Publish"**

---

### 3. Якщо Storage взагалі не створений

1. Відкрийте [Firebase Console](https://console.firebase.google.com/)
2. Виберіть ваш проект
3. В меню зліва: **Build** → **Storage**
4. Натисніть **"Get started"**
5. Виберіть **"Start in production mode"**
6. Виберіть локацію (найближчу до вас, наприклад: `europe-west`)
7. Натисніть **"Done"**
8. Перейдіть на вкладку **"Rules"** і вставте правила з кроку 2

---

### 4. Перевірка

1. Оновіть сторінку адмін-панелі (Ctrl+F5)
2. Спробуйте завантажити зображення
3. В консолі (F12) мають з'явитися логи:
   ```
   Starting upload: image.jpg to folder: articles
   Uploading to path: articles/1234567890-image.jpg
   Upload complete, getting download URL...
   Download URL: https://firebasestorage.googleapis.com/...
   Upload successful!
   ```

---

## 🆘 Додаткова діагностика

### Перевірте чи Storage ініціалізований:

Відкрийте консоль браузера (F12) і введіть:

```javascript
console.log('Storage:', window.firebase?.storage);
```

Має показати об'єкт Storage, а не `undefined`.

### Перевірте чи користувач авторизований:

```javascript
firebase.auth().currentUser
```

Має показати об'єкт користувача з email.

---

## ✅ Після налаштування

Завантаження зображень має працювати:
1. Перетягніть фото або натисніть на область
2. Побачите прогрес-бар
3. Через 2-3 секунди з'явиться превью зображення
4. URL зображення автоматично збережеться

---

**Якщо проблема залишається:**
1. Очистіть кеш браузера (Ctrl+Shift+Delete)
2. Перезавантажте сторінку (Ctrl+F5)
3. Перевірте консоль (F12) на помилки
4. Переконайтеся що ви авторизовані в адмін-панелі



