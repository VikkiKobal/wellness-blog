# 🔥 Швидке налаштування Firestore (3 хвилини)

## ❗ ВАЖЛИВО
Без Firestore кнопка Save Article/Course не працюватиме!

---

## 🚀 Швидке налаштування:

### Крок 1: Відкрийте Firebase Console

https://console.firebase.google.com/

Виберіть проект: **wellness-website-b150a**

---

### Крок 2: Створіть Firestore Database

1. В меню зліва: **Build** → **Firestore Database**
2. Натисніть велику кнопку **"Create database"**
3. Оберіть режим: **"Start in test mode"** (для швидкого старту)
4. Натисніть **"Next"**
5. Оберіть локацію (найближчу до вас):
   - Європа: `europe-west` або `europe-central2`
   - США: `us-central`
6. Натисніть **"Enable"**

Почекайте 30 секунд поки база створюється...

---

### Крок 3: Налаштуйте правила (для продакшену)

Після створення перейдіть на вкладку **"Rules"** і замініть на:

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Статті
    match /articles/{article} {
      allow read: if true;
      allow write: if request.auth != null;
    }
    
    // Курси
    match /courses/{course} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
```

Натисніть **"Publish"**

---

### Крок 4: Перевірка

1. Оновіть адмін-панель (Ctrl+F5)
2. Заповніть форму статті
3. Натисніть **"Save Article"**
4. В консолі (F12) побачите:
   ```
   saveArticle called!
   Form found, getting data...
   Article data: {...}
   Creating new article...
   Article created with ID: abc123
   ```
5. З'явиться зелений попап: **"✓ Article created successfully!"**

---

## 🎯 Все готово!

Після цього:
- ✅ Save Article працюватиме
- ✅ Save Course працюватиме
- ✅ Edit/Delete працюватиме
- ✅ Статті зберігатимуться в Firestore
- ✅ Курси зберігатимуться в Firestore

---

## ⚠️ Важливі примітки:

### "Test mode" - що це?

В test mode всі можуть **читати** дані, але **писати** можуть тільки авторизовані користувачі.

**Це безпечно** для вашого блогу, бо:
- Статті публічні (всі їх читають)
- Редагувати можуть тільки ті, хто увійшов в адмін-панель

### Якщо побачите помилку "permission-denied"

Переконайтеся що:
1. Ви авторизовані в адмін-панелі
2. Rules опубліковані в Firebase Console
3. Ви оновили сторінку після входу (Ctrl+F5)

---

## 🔍 Діагностика

Якщо Save все ще не працює, відкрийте консоль (F12) і побачите детальні логи:

```
saveArticle called!           ← Форма відправлена
Form found, getting data...   ← Дані зібрані
Article data: {...}           ← Показує всі дані
Creating new article...       ← Відправка в Firestore
Article created with ID: xxx  ← Успіх!
```

Якщо побачите помилку - скопіюйте текст помилки і я допоможу!

---

**Налаштуйте Firestore за 3 хвилини і Save запрацює! 🚀**



